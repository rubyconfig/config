language: ruby
cache: bundler
sudo: false
rvm:
  - 2.4.5
  - 2.5.3
  - 2.6.0
  - 2.7.0
  - truffleruby
gemfile:
  - gemfiles/rails_4.2.gemfile
  - gemfiles/rails_5.0.gemfile
  - gemfiles/rails_5.1.gemfile
  - gemfiles/rails_5.2.gemfile
  - gemfiles/sinatra.gemfile
matrix:
  exclude:
    # In Ruby 2.7 we can't remove bundler 2.x because it's a default gem.
    # This in turn interacts badly with rails 4.2.
    - rvm: 2.7.0
      gemfile: gemfiles/rails_4.2.gemfile
    # truffleruby does not seem to work with Rails
    - rvm: truffleruby
      gemfile: gemfiles/rails_4.2.gemfile
    - rvm: truffleruby
      gemfile: gemfiles/rails_5.0.gemfile
    - rvm: truffleruby
      gemfile: gemfiles/rails_5.1.gemfile
    - rvm: truffleruby
      gemfile: gemfiles/rails_5.2.gemfile
before_install:
  - gem uninstall -v '>= 2' -i $(rvm gemdir)@global -ax bundler || true
  - gem install bundler -v '< 2'
script:
  - bundle exec rspec
addons:
  code_climate:
    repo_token: e233a382471f480a061ac3098edadbb91cee8129e0938973aeb2aa8d84a0a93b
after_success:
  - bundle exec codeclimate-test-reporter
