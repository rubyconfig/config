engines:
  duplication:
    enabled: true
    config:
      languages:
        ruby:
          mass_threshold: 18
  flog:
      enabled: true
  git-legal:
    enabled: true
  markdownlint:
    enabled: true
  reek:
    enabled: true
  rubocop:
    enabled: true

ratings:
   paths:
   - Gemfile.lock
   - "**.md"
   - "**.rb"

exclude_paths:
- "spec/app/**/*"
